spring:
  application:
    name: config-server-service
  profiles:
    active: native
  cloud:
    config:
      server:
        git:
          uri: https://github.com/papealphou1986/gmp_configs
    discovery:
      enabled: true
server:
  port: 8888