spring:
  application:
    name: config-server-service
  profiles:
    active: native
  cloud:
    config:
      server:
        git:
          url: https://github.com/papealphou1986/gmp_configs   # Configuration Git repository URI for the Config Server
    discovery:
      enabled: true
server:
  port: 8888